tenants:
  admin:
    create: false
    project: admin

    apps:
      monitoring:
        path: apps/monitoring
        targetRevision: HEAD
        wave: -5
        extraFields: |
          ignoreDifferences:
          - group: 
            kind: 
            jsonPointers:
            - /metadata/annotations/"helm.sh/hook"